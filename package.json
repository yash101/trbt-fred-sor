{
  "name": "trbt-fred-sor",
  "version": "1.0.0",
  "description": "gRPC SoR for FRED Economic Data (public)",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node build/main/index.js",
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.5",
    "@grpc/proto-loader": "^0.7.10",
    "@types/node": "^20.8.6",
    "google-protobuf": "^3.21.2",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
